import React from "react";
import { useMeiadQuery } from "react-responsive";

const viewSize = () => {
    const notebook = useMediaQuery({ maxWidth: 965 })
}

return (
    <div></div>
)

export default reactResponsive;